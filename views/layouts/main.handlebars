<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8"> <!-- Set character encoding -->
    <meta name="viewport" content="width=device-width, initial-scale=1.0"> <!-- Responsive design for mobile devices -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link rel="stylesheet" href="/css/style.css"> <!-- Link to external CSS -->
    <title>{{title}}</title> <!-- Dynamic title for each page -->
</head>
<body>
    <header class="tech-header">
        <div class="logo-container">
            <img src="/images/logo.png" alt="Tech Blog Logo" class="logo">
            <h1 class="site-title">Tech-Yak</h1>
        </div>
        <nav class="nav-icons">
            <a href="/" title="Home">
                <i class="fas fa-home"></i>Home
            </a>
            <a href="/about" title="About">
                <i class="fas fa-info-circle"></i>About
            </a>
            {{#if logged_in}} <!-- If the user is logged in, show Dashboard and Logout options -->
            <a href="/dashboard" title="Dashboard">
                <i class="fas fa-user-circle"></i>Dashboard
            </a>
            <button type="submit" class="logout-btn" id="logout">
                <i class="fas fa-sign-out-alt"></i> Logout</button><!-- Logout Button -->
            {{else}} <!-- If the user is not logged in, show Login and Signup options -->
            <a href="/login" title="Login">
                <i class="fas fa-sign-in-alt"></i>Login
            </a>
            <a href="/signup" title="Signup">
                <i class="fas fa-user-plus"></i>Sign Up
            </a>
            {{/if}}
            <button class="toggle-button" id="theme-toggle">
                <i class="fas fa-moon"></i> Dark Mode
            </button>
        </nav>
    </header>

    <div class="container">
        {{{body}}} <!-- Render the specific view content here -->
    </div>

    <footer>
        <p>CMS Blog Site &copy; 2024</p>
    </footer>

    {{#if js}} <!-- If a JavaScript file is specified, include it -->
    <script src="/js/{{js}}.js"></script>
    {{/if}}

    {{#if logged_in}}
    <script src="/js/logout.js"></script> <!-- Load logout.js if the user is logged in -->
    {{/if}}
    <script src="/js/theme-toggle.js"></script>
    <script src="/js/comment.js"></script>
</body>
</html>
