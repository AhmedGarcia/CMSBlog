{{#each posts}}
  <div class="post">
    <h3>{{title}}</h3>
    <p>{{format_date createdAt}}</p>
    <button data-id="{{id}}">Edit</button>
    <button data-id="{{id}}">Delete</button>
  </div>
{{/each}}